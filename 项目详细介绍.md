# JobClaw 项目详细介绍

## 一、项目概述

JobClaw 是一个 Chrome 浏览器扩展（Manifest V3），用于在 **BOSS 直聘**（zhipin.com）上自动化求职。它的核心功能是：

1. **简历解析** → 上传 PDF/DOCX/TXT 简历，自动识别文本
2. **AI 职业画像** → 通过 AI 从简历中提取求职方向、技能、硬条件
3. **岗位方向规划** → 基于画像生成可编辑的投递方向列表
4. **自动搜索与采集** → 在 BOSS 直聘上按关键词自动搜索岗位
5. **AI 岗位匹配** → 对每个岗位进行 AI 分析，打分、判定是否推荐
6. **自动/人工投递** → 支持全自动投递和人工确认两种模式
7. **会话安全保护** → 确保只向正确的 HR 发送，不串话、不重复投递

---

## 二、项目结构

```
job-claw/
├── source/                          # 源码目录
│   ├── src/                         # 核心源代码
│   │   ├── background.js            # Service Worker：任务编排、AI、存储、重试
│   │   ├── common.js                # 默认配置、工具函数
│   │   ├── content-v37.js           # BOSS 页面注入脚本：岗位识别、聊天操作
│   │   ├── sidepanel.js             # 侧边栏 UI：4 个页面交互逻辑
│   │   ├── offscreen.js             # 离屏文档：剪贴板写入
│   │   ├── pdf-extractor.js         # PDF 纯浏览器端解析引擎
│   │   └── lib/
│   │       ├── conversation-identity.js  # HR/公司会话身份与投递锁
│   │       ├── job-priority.js           # 岗位优先级排序算法
│   │       └── task-state.js             # 任务阶段和进度元数据
│   ├── public/                      # 静态资源
│   │   ├── manifest.json            # Chrome 扩展清单
│   │   ├── sidepanel.html           # 侧边栏 HTML
│   │   ├── offscreen.html           # 离屏文档 HTML
│   │   └── styles.css               # 全局样式（OKLCH 色彩空间）
│   ├── tests/                       # 测试
│   │   ├── unit/                    # 单元测试
│   │   ├── run-unit.mjs             # 单元测试入口
│   │   ├── run-integration.mjs      # 集成测试入口
│   │   ├── validate.mjs             # Manifest/CSP/HTML 结构验证
│   │   ├── check-syntax.mjs         # 语法检查
│   │   └── ui*.mjs                  # 大量回归测试（UI9—UI37）
│   ├── scripts/                     # 构建辅助脚本
│   │   ├── check-manifest.mjs
│   │   ├── check-release-sync.mjs
│   │   ├── check-syntax.mjs
│   │   └── scan-secrets.mjs
│   └── build.mjs                    # 构建脚本（复制 source/ → chrome-extension/）
├── chrome-extension/                # 构建产物（与 source/ 同步）
├── skills/hallmark/                 # Hallmark 设计 Skill（OKLCH 色彩指南）
├── docs/                            # 文档和截图
├── AI_IMPLEMENTATION_PLAN.md        # 开发计划与阶段历史
└── package.json                     # 项目配置
```

---

## 三、核心模块详解

### 3.1 background.js — Service Worker（任务编排中心）

**职责**：整个扩展的"大脑"，负责所有后端逻辑

**主要功能块**：

| 功能块 | 说明 |
|--------|------|
| `callModel()` | AI API 调用核心（第 1408 行），支持 OpenAI 兼容接口 |
| `buildProfile()` | 职业画像生成（AI + 本地 fallback） |
| `analyzeJob()` | 岗位匹配分析 |
| `buildLocalProfile()` | 本地规则画像生成（AI 不可用时的兜底） |
| `buildDirectionPlan()` | 岗位方向规划生成 |
| `createTasks()` | 根据方向 × 关键词 × 地区 × 类型生成搜索任务 |
| `dispatchNextAutoPending()` | 全自动模式下的投递调度 |
| `approvePending()` / `approveAllPending()` | 人工确认投递 |
| 消息处理（第 2595 行起） | 处理所有来自 sidepanel/content 的消息 |
| Debugger 操作 | 通过 Chrome Debugger API 实现可信输入（粘贴、点击、键盘事件） |
| 剪贴板操作 | 通过 offscreen document 实现跨上下文剪贴板写入 |

**消息类型映射**：

| 消息类型 | 触发方 | 功能 |
|----------|--------|------|
| `BUILD_PROFILE` | sidepanel | 调用 AI 生成职业画像 |
| `AI_JOB` | content | 调用 AI 分析岗位匹配 |
| `TEST_AI` | sidepanel | 测试 AI 连接 |
| `REBUILD_DIRECTION_PLAN` | sidepanel | 重新生成岗位方向 |
| `START` | sidepanel | 启动搜索/投递任务 |
| `PAUSE` / `STOP` | sidepanel | 暂停/停止任务 |
| `APPROVE` / `APPROVE_ALL` | sidepanel | 确认待投递岗位 |
| `CHAT_BINDING_PREPARE` | content | 建立岗位→HR 会话绑定 |
| `CHAT_BINDING_CHECK` | content | 检查会话冲突 |
| `CHAT_BINDING_CONFIRMED` | content | 确认会话绑定 |
| `APPLY_COMPLETE` | content | 投递完成/失败回调 |
| `TRUSTED_CHAT_INPUT` | content | 通过 CDP 执行可信输入 |
| `TASK_PROGRESS` | content | 更新投递任务进度 |
| `SEARCH_TASK_PROGRESS` | content | 更新搜索任务进度 |
| `PENDING` | content | 添加待确认岗位 |
| `STATS` | content | 更新统计数据 |
| `EVENT` | content | 写入运行日志 |

### 3.2 content-v37.js — BOSS 页面注入脚本

**职责**：在 BOSS 直聘页面中执行所有 DOM 操作

**核心类 `BossAdapter`** 提供的方法：

| 类别 | 方法 | 功能 |
|------|------|------|
| 页面识别 | `pageType()` | 判断当前页面类型（jobs/chat/verification） |
| 岗位搜索 | `searchInput()`, `searchButton()`, `cards()`, `applySearchTask()` | 搜索框操作、岗位卡片获取、筛选条件应用 |
| 岗位详情 | `detailRoot()`, `openCard()`, `extractJob()`, `commmunicateButton()` | 打开岗位详情、提取岗位信息 |
| 聊天操作 | `chatInput()`, `enterChat()`, `sendGreeting()`, `setChatText()` | 聊天输入框定位、进入聊天、发送招呼语 |
| 会话安全 | `chatContext()`, `validateChatContext()`, `assertConversationKey()`, `ensureExpectedConversation()` | 会话身份验证、防止串话 |
| 消息确认 | `chatMessageSnapshot()`, `greetingVisibleInChat()`, `waitForStableOutgoingGreeting()` | 确认招呼语已出现在聊天记录 |
| 附件操作 | `uploadResumeImage()`, `sendOnlineResume()` | 发送简历图片、在线简历 |
| 页面导航 | `returnToJobsHome()`, `ensureJobsPage()`, `homeNavigationButton()` | 返回主页、打开岗位页 |
| 外部网申 | `externalApplicationInfo()`, `isExternalApplicationJob()` | 检测并跳过外部网申岗位 |

**主流程函数**：

- `processSearch(state)` — 搜索→采集→AI分析→入队的主循环
- `processApproved(state)` — 打开岗位→进入聊天→发招呼语→发附件→确认结果
- `run()` — 顶层调度，根据状态选择 `processSearch` 或 `processApproved`

### 3.3 sidepanel.js — 侧边栏 UI

**职责**：4 个页面的 UI 交互逻辑

**页面结构**：

| 页面 | 功能 |
|------|------|
| 首页（home） | 任务状态概览、进度条、运行日志、启动/暂停/停止 |
| 简历（resume） | 简历原文编辑、职业画像生成、岗位方向选择 |
| 消息（messages） | 待确认岗位列表、投递任务进度、批量操作 |
| 设置（settings） | AI 配置、搜索条件、执行模式（人工/自动） |

**核心功能函数**：

| 函数 | 功能 |
|------|------|
| `saveResume()` | 保存简历原文 |
| `buildProfile()` / `regenerateProfile()` | 触发 AI 画像生成 |
| `saveProfile()` | 保存职业画像 |
| `renderDirectionPlan()` | 渲染岗位方向列表 |
| `saveDirectionPlan()` | 保存并应用岗位方向 |
| `renderDeliveryTasks()` | 渲染投递任务进度 |
| `renderPending()` | 渲染待确认岗位列表 |
| `createQueueItem()` | 创建单个待确认岗位卡片（含"打开岗位"按钮） |
| `saveSettings()` | 保存设置（含 AI 配置） |
| `setExecutionMode()` | 切换人工/自动模式 |

### 3.4 lib/ — 工具库

| 文件 | 功能 |
|------|------|
| `conversation-identity.js` | HR 身份归一化（去除公司类型后缀、HR 标签等），会话投递锁生成 |
| `job-priority.js` | 岗位优先级计算：AI 评分 × 决策修正 + 薪资 + 新鲜度 + 惩罚项 |
| `task-state.js` | 任务阶段枚举（`TASK_STAGE_META`），共 20 个阶段 |

### 3.5 pdf-extractor.js — PDF 解析引擎

纯浏览器端 PDF 解析，不依赖任何外部服务：
- 支持 FlateDecode、ASCIIHexDecode、ASCII85Decode、RunLengthDecode
- 支持 ToUnicode CMap 字体映射（中文 PDF 关键功能）
- 支持 ObjStm 压缩对象流
- 支持 Marked Content（ActualText/Alt）结构化文本提取
- 输出可读性评分（`readableScore`）和简历语义检测（`isReadableResumeText`）

### 3.6 offscreen.js — 剪贴板操作

独立于 Service Worker 的离屏文档，用于在 Manifest V3 中实现 `navigator.clipboard.writeText()`。

---

## 四、AI 相关流程

### 4.1 整体 AI 调用架构

```
┌─────────────┐    消息通信     ┌──────────────┐    HTTP POST    ┌──────────────┐
│  sidepanel  │ ──────────────→ │  background  │ ──────────────→ │  AI API      │
│   (UI层)     │ ←────────────── │  (Service    │ ←────────────── │  (DeepSeek   │
│              │    chrome.runtime│  Worker)     │   JSON响应     │   兼容接口)   │
└─────────────┘                 └──────────────┘                 └──────────────┘
       ↑                               ↑
       │                               │
       │          chrome.runtime       │
       │                               │
┌──────┴────────┐              ┌──────┴──────────┐
│  content-v37  │              │  offscreen      │
│  (BOSS页面)    │              │  (剪贴板)        │
└───────────────┘              └─────────────────┘
```

### 4.2 AI 配置

```javascript
// common.js 中的默认配置
model: {
  baseUrl: 'https://api.deepseek.com',      // 兼容 OpenAI 接口格式
  apiKey: '',                                // 用户在设置页填写
  model: 'deepseek-v4-pro',                  // 默认模型
  temperature: 0.1                           // 低温度保证一致性
}
```

用户可在设置页自定义：
- `baseUrl`：API 地址（支持任何 OpenAI 兼容接口）
- `model`：模型名称
- `apiKey`：API 密钥

### 4.3 AI API 调用机制 (`callModel`)

```javascript
// background.js 第 1408 行
async function callModel(messages, jsonMode = true, options = {}) {
  // 1. 读取用户配置的 API Key
  // 2. 构造 POST 请求到 {baseUrl}/chat/completions
  // 3. 请求体：
  //    - model: 用户配置的模型名
  //    - messages: 传入的对话数组
  //    - temperature: 默认 0.1
  //    - max_tokens: 默认 2800
  //    - response_format: { type: 'json_object' }（jsonMode 时）
  // 4. 超时：默认 90000ms
  // 5. 兼容处理：如果 API 不支持 response_format，自动重试（去掉该字段）
  // 6. JSON 解析：提取 choices[0].message.content
  // 7. JSON 模式：额外调用 extractJson() 提取 JSON（处理 markdown 代码块包裹）
  // 8. 错误分类：
  //    - AI_CONFIG：API Key 未配置
  //    - AI_HTTP：HTTP 错误
  //    - AI_INVALID_RESPONSE：无法解析响应
  //    - AI_EMPTY：返回为空
  //    - AI_TRUNCATED：输出被截断（finish_reason === 'length'）
  //    - AI_INVALID_JSON：JSON 不完整
  //    - AI_PROFILE_INCOMPLETE：画像字段不完整
  //    - AI_SERVICE_DOWN：fetch 失败
  //    - AI_UNKNOWN：未知错误
}
```

### 4.4 AI 调用流程总览

```
用户上传简历
    │
    ▼
┌─────────────────────────────────────────────┐
│ 1. 简历解析                                  │
│    - PDF: pdf-extractor.js (纯浏览器端)       │
│    - DOCX: ZIP → document.xml 提取           │
│    - TXT: 直接读取                            │
└──────────────────────┬──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│ 2. 职业画像生成 (BUILD_PROFILE)               │
│    ┌─ AI 完整画像（buildProfile）              │
│    │  ├─ 成功 → 合并本地 fallback              │
│    │  └─ 失败 → isRetryableAiOutputError?     │
│    │      ├─ 是 → AI 精简重试（compact）       │
│    │      │    ├─ 成功 → 采用精简结果          │
│    │      │    └─ 失败 → 本地规则画像          │
│    │      └─ 否 → 本地规则画像                 │
│    └─ 本地规则画像（buildLocalProfile）        │
│       - 正则提取技能（40+ 技能库）             │
│       - 正则提取学历、城市、求职方向           │
│       - 根据方向预设生成关键词                  │
└──────────────────────┬──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│ 3. 岗位方向规划 (buildDirectionPlan)           │
│    - 基于画像的 primaryDirections 生成         │
│    - 匹配 DIRECTION_PRESETS（6 个预设）       │
│    - 自动计算匹配技能、缺口、理由              │
│    - 用户可编辑、添加自定义方向                │
│    - 保存后生成搜索任务（方向 × 关键词 × 地区 × 类型）│
└──────────────────────┬──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│ 4. 岗位搜索与采集 (processSearch)             │
│    - 在 BOSS 页面执行搜索任务                 │
│    - 遍历岗位卡片，打开详情页                  │
│    - 提取岗位信息（标题、公司、薪资、描述等）  │
│    - 检测外部网申岗位并跳过                    │
└──────────────────────┬──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│ 5. AI 岗位匹配分析 (AI_JOB → analyzeJob)     │
│    输入：职业画像 + 简历 + 岗位信息           │
│    输出：{score, decision, hardBlocks,        │
│            matchedEvidence, gaps, risks,      │
│            reason, greeting}                  │
│    - decision: recommend | cautious | reject  │
│    - 硬条件不通过 → 强制 reject               │
│    - 分数 < minScore → 降级为 cautious        │
│    - 推荐 + 分数达标 → 入队                   │
└──────────────────────┬──────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────┐
│ 6. 投递处理                                  │
│    ┌─ 人工模式：待确认列表 → 用户确认 → 投递  │
│    └─ 自动模式：                              │
│       - 积累 5 个合格岗位后按优先级排序        │
│       - 按优先级逐个投递                       │
│       - 首次成功后自动暂停（验收机制）         │
│    ┌─ 投递流程：                              │
│    │  1. 打开岗位详情页                       │
│    │  2. 点击"立即沟通"进入聊天               │
│    │  3. 验证 HR 身份（会话安全）             │
│    │  4. 填写 AI 生成的招呼语                 │
│    │  5. 发送招呼语（多通道兜底）             │
│    │  6. 确认消息出现在聊天记录               │
│    │  7. 发送简历图片/在线简历（可选）        │
│    │  8. 返回主页继续搜索                     │
└─────────────────────────────────────────────┘
```

---

## 五、AI 提示词详解

### 5.1 职业画像生成 — 完整版（`buildProfile`）

**触发时机**：用户点击"保存并生成画像"或"从简历重建初稿"

**System Prompt**（第 1767 行）：

```
你是严格的职业画像分析器。只能使用简历真实事实，不能根据项目业务场景推断用户职业。
主方向最多3个，搜索词必须是真实岗位名称。数组必须精简，教育/经历/项目各最多4条，
每条不超过80字，技能最多15个，摘要不超过180字。即使信息不完整，也必须给出可编辑初稿，
禁止返回空内容。输出严格 JSON：
{"facts":{"education":[],"experiences":[],"projects":[],"skills":[],"certificates":[]},
 "primaryDirections":[{"name":"","confidence":0,"evidence":[]}],
 "secondaryDirections":[],"searchKeywords":[],"hardConstraints":{"locations":[],
 "employmentTypes":[],"salary":"","experience":"","degree":""},
 "excludeDirections":[],"summary":""}
```

**User Message**：简历文本（截取前 22000 字符）

**参数**：`maxTokens: 4200, temperature: 0.05`

**后处理**：
- `validateGeneratedProfile()` 校验：主方向、搜索词、摘要、facts、hardConstraints 必须非空
- `mergeProfileWithFallback()` 合并：AI 字段优先，空字段用本地规则补齐

---

### 5.2 职业画像生成 — 精简版（compact retry）

**触发时机**：完整版 AI 输出失败（JSON 不完整、截断、字段缺失）时自动重试

**System Prompt**（第 1783 行）：

```
你是求职职业画像分析器。只使用简历事实。请输出极简 JSON，不要解释，不要证据长句。
摘要120字以内；主方向最多3个；搜索词最多10个；技能最多12个；其余字段简短。
输出结构：{"summary":"","primaryDirections":[],"searchKeywords":[],"skills":[],
"locations":[],"employmentTypes":[],"salary":"","experience":"","degree":"",
"excludeDirections":[]}
```

**User Message**：简历文本（截取前 18000 字符）

**参数**：`maxTokens: 2200, temperature: 0.05`

**后处理**：
- `validateCompactGeneratedProfile()` 校验
- `compactProfileToFull()` 转回完整结构体
- 合并本地 fallback

---

### 5.3 岗位匹配分析（`analyzeJob`）

**触发时机**：每采集到一个岗位详情时自动调用

**System Prompt**（第 2190 行）：

```
你是为求职者服务的岗位匹配审查器，不是招聘方。用户是正在应聘岗位的求职者。
任何能力、年限、项目和成果都不能超出简历事实。
先判断学历、经验、地点等硬条件，再判断方向与技能。
输出 JSON：
{"score":0,"decision":"recommend|cautious|reject","hardBlocks":[],"matchedEvidence":[],
 "gaps":[],"risks":[],"reason":"","greeting":""}。
greeting 是求职者发给招聘方/HR 的第一人称求职招呼语，50-110字，
应使用"您好，我想应聘贵公司的……"口吻。
严禁写成招聘方口吻，严禁出现"看到你的简历""你的经历很匹配我们""欢迎进一步沟通"
"我们团队""候选人"等表述；不得承诺薪资、到岗、年限或不存在的能力。
```

**User Message**：

```
职业画像：{JSON.stringify(profile)}
简历：{resumeText.slice(0, 15000)}
岗位：{JSON.stringify(job)}
```

**后处理**：

| 步骤 | 说明 |
|------|------|
| `score` 截断 | 0-100 范围 |
| `greeting` 校验 | `normalizeApplicantGreeting()` 检测招聘方口吻，不合格则用 `fallbackApplicantGreeting()` |
| 硬条件检测 | 有 `hardBlocks` → 强制 `decision = 'reject'` |
| 分数阈值 | 分数 < `minScore`（默认 75）且 decision 为 recommend → 降级为 cautious |

**招呼语兜底逻辑**（`normalizeApplicantGreeting`）：

```javascript
// 检测招聘方口吻的关键词：
const reversed = /看到你的简历|你的简历|很匹配我们|匹配我们|欢迎.*沟通|期待你|候选人|我们团队|我们公司|团队主要涉及|你很匹配|方便的话来聊聊/i;
// 检测求职者口吻：
const applicantVoice = /我想应聘|我希望应聘|我对.{0,20}(岗位|职位).{0,10}(感兴趣|有兴趣)|想进一步了解|希望进一步沟通/;
// 不合格 → 使用本地兜底模板：
function fallbackApplicantGreeting(job, profile) {
  return `您好，我想应聘贵公司的${title}岗位。${evidence}，对岗位的工作内容很感兴趣。方便的话希望进一步了解岗位和团队，谢谢。`;
}
```

---

### 5.4 AI 连接测试（`TEST_AI`）

**触发时机**：用户在设置页点击"测试连接"

**System Prompt**（第 2706 行）：

```
只回复"连接正常"。
```

**User Message**：`测试连接`

**模式**：非 JSON 模式（`jsonMode = false`），直接返回纯文本

---

### 5.5 岗位方向规划（`buildDirectionPlan`）

**注意**：此功能**不使用 AI**，完全基于本地规则和预设

**实现方式**：
1. 从画像的 `primaryDirections` 和 `searchKeywords` 提取候选方向
2. 匹配 `DIRECTION_PRESETS`（6 个预设：前端、AI应用、全栈、开发者工具、数据可视化、后端）
3. 根据预设生成匹配技能、缺口技能、推荐搜索关键词
4. 用户自定义方向保留并合并

---

## 六、安全性机制

### 6.1 会话安全（防止串话）

| 机制 | 说明 |
|------|------|
| 会话身份归一化 | 去除公司后缀（"有限责任公司"等）、HR 标签（"招聘者"、"刚刚活跃"） |
| 会话键生成 | `hr:{recruiterName}|company:{company}` 作为唯一标识 |
| 投递锁（Delivery Ledger） | 每个 HR 会话同时只能绑定一个岗位任务 |
| 会话验证 | 发送前验证：HR 姓名匹配 + 左侧会话选中 + 编辑器就绪 |
| 明确冲突检测 | 页面明确显示其他公司/岗位 → 禁止发送 |
| 聊天记录确认 | 发送后等待消息气泡出现在聊天记录中，确认后才返回主页 |

### 6.2 其他安全机制

- **外部网申跳过**：检测"立即网申"/"去网申"按钮，跳过非 BOSS 聊天投递的岗位
- **安全验证暂停**：检测 BOSS 安全验证页面，自动暂停任务
- **首次单条验收**：全自动模式下，首次成功投递后自动暂停，用户确认后再继续
- **每日上限**：达到 `dailyTarget`（默认 150）后自动停止
- **招呼语反招聘方检测**：确保 AI 生成的招呼语是求职者口吻
- **扩展上下文失效检测**：检测 `Extension context invalidated`，静默停止

---

## 七、投递流程的容错设计

招呼语发送采用多通道兜底策略：

```
优先级 1: 可信通道（Chrome Debugger API）
  ├─ 主世界写入（MAIN world scripting）
  ├─ 剪贴板粘贴（Ctrl+V via CDP）
  └─ CDP 逐字输入（dispatchKeyEvent char）
  
优先级 2: DOM 输入兜底
  ├─ 原生 value 设置（setValue）
  ├─ contenteditable 的 execCommand('insertText')
  └─ textContent 替换
  
发送触发：
  优先级 1: CDP Enter（debuggerPressEnter）
  优先级 2: DOM Enter（dispatchEvent KeyboardEvent）
  优先级 3: 发送按钮点击（sendButton 定位 + click）
```

消息确认：发送后等待新消息气泡出现在聊天记录中，连续 3 次采样稳定后才确认成功。

---

## 八、优化建议汇总

基于对现有 AI 提示词和流程的分析，以下是可以优先考虑的优化方向：

### 8.1 Prompt 优化

| 问题 | 位置 | 建议 |
|------|------|------|
| **buildProfile 指令过载** | 第 1767 行 | 一条 prompt 包含 10+ 条约束，LLM 可能忽略部分。建议分层：先提取事实，再推断方向 |
| **负向指令过多** | 第 1767/2190 行 | 大量"禁止"、"严禁"、"不得" → 可改为正向引导 + 少量关键禁止 |
| **analyzeJob 评分标准模糊** | 第 2190 行 | 没有明确的评分 rubric（如：硬条件匹配 +30，技能匹配 +20 等），AI 打分缺乏一致性 |
| **缺少 few-shot 示例** | 所有 prompt | 全部是 zero-shot，没有示例。添加 1-2 个 few-shot 示例可显著提升输出质量 |
| **中英混合** | 第 2190 行 | System prompt 中文，但字段名英文（score/decision/hardBlocks），模型可能混淆 |
| **招呼语长度限制矛盾** | 第 2190 行 | prompt 要求 50-110 字，但代码 `slice(0, 160)` 截断 160 字，不一致 |

### 8.2 架构优化

| 问题 | 建议 |
|------|------|
| **单次 API 调用做太多事** | `analyzeJob` 同时做评分、决策、理由、招呼语生成。建议拆分为：评分分析 + 招呼语生成 两个独立调用 |
| **画像重试策略固定** | 精简版 prompt 固定，可以根据首次失败原因动态调整（如：JSON 截断→降低 maxTokens、字段缺失→简化 schema） |
| **本地画像正则库有限** | 技能库 40+ 个，城市库 28 个，覆盖有限。可考虑让 AI 作为"增强"而非"替代"本地规则 |
| **无 AI 调用缓存** | 同一简历重复生成画像会重新调用 AI。可对简历内容做 hash 缓存 |
| **无流式输出** | 当前只支持非流式（一次返回完整 JSON），不支持 SSE streaming |

### 8.3 流程优化

| 问题 | 建议 |
|------|------|
| **岗位匹配无批量处理** | 每个岗位单独调用 AI，搜索 100 个岗位 = 100 次 API 调用。可考虑批量分析（一次送多个岗位） |
| **无 AI 调用频率控制** | 没有速率限制，大量岗位可能触发 API 限流 |
| **招呼语无个性化** | 招呼语模板固定，可考虑根据岗位描述中的关键词动态调整 |
| **方向规划不用 AI** | 当前纯本地规则，可考虑用 AI 生成更精准的方向推荐 |

### 8.4 测试覆盖

| 问题 | 建议 |
|------|------|
| **无 AI 输出质量测试** | 没有对 AI 提示词效果的 A/B 测试框架 |
| **无 token 消耗监控** | 没有统计每次调用的 token 消耗和成本 |

---

## 九、技术栈总结

| 类别 | 技术 |
|------|------|
| 运行环境 | Chrome Extension Manifest V3 |
| 语言 | JavaScript (ES Module) |
| AI 接口 | OpenAI 兼容 API（默认 DeepSeek） |
| UI | Vanilla JS + CSS（OKLCH 色彩空间） |
| 页面操作 | Content Script + Chrome Debugger API |
| 存储 | chrome.storage.local |
| 测试 | Node.js 内置 test runner |
| 构建 | 自定义 build.mjs（复制+校验） |
| 无外部依赖 | 零 npm 运行时依赖 |